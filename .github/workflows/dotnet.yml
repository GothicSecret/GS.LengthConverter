# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches: [ "dev", "**" ]
  pull_request:
    branches: [ "dev" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 6.0.x
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build --no-restore
    - name: Test
      run: dotnet test --no-build --verbosity normal

    - uses: nuget/setup-nuget@v1
      with:
        nuget-api-key: ${{ secrets.NuGetAPIKey }}
        nuget-version: latest
    - run: ls /
    - run: ls /home
    - run: ls /home/runner/
    - run: ls /home/runner/work/
    - run: ls /home/runner/work/GS.LengthConverter/
    - run: ls ./GS.LengthConverter.Net6/
    - run: ls ./GS.LengthConverter.Net6/bin/
    - run: ls ./GS.LengthConverter.Net6/bin/Debug
    - run: ls ./GS.LengthConverter.Net6/bin/Debug/net6.0
    - run: nuget restore ./GS.LengthConverter.sln
    - run: nuget pack ./nuspec/GS.LengthConverter.nuspec